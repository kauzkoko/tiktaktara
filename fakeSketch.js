let scaler = 60
let w = 9 * scaler
let h = 16 * scaler
let tickAngle = 6
let tickTotal = 0
let tickSpeed = 60 //60 equals 1 tick per second when used default 60fps
let t, i, k, a

function setup() {
	//noCanvas()
}
